$(function() {
  const typed = new Typed('#typed', {
    strings: ['Trimm Application', 'Act for Better Life!'],
    typeSpeed: 100,
    startDelay: 300,
    backDelay: 1500,
    backSpeed: 100,
    loop: true,
    loopCount: Infinity,
  });
});
